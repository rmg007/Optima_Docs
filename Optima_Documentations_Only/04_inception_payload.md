# 04 Inception Payload

This document contains the exact content Optima generates in host projects. Copy these strings verbatim into the generator modules.

## File: `.claude/rules/optima-feedback.md`

Generated by `src/generator/feedback-rules.ts`. Written on first `optima_get_context` call (cold start bootstrap step 3e). Regenerated after every `optima_reindex` call (step 7). Content is static for MVP — regeneration is a no-op unless the file was deleted. Phase 2 may add dynamic content (path-scoped rules).

````markdown
---
paths: "**/*"
---

**Violating the letter of these rules is violating the spirit of these rules.**

If you find yourself arguing "the spirit of Optima is understanding the codebase, and I already understand it" — that IS the rationalization these rules exist to prevent. Call the tools. Every time. No exceptions.

## Instruction Priority

When instructions conflict, follow this priority order:

1. **Developer's explicit instructions** (verbal commands, CLAUDE.md content OUTSIDE Optima markers) — highest priority
2. **Optima-generated CLAUDE.md content** (inside `<!-- OPTIMA:START -->` markers) — project-learned context
3. **This feedback rules file** (`.claude/rules/optima-feedback.md`) — tool usage instructions
4. **Claude Code's default behavior** — lowest priority

**Examples:**
- Developer says "don't use Optima for this task" → Skip Optima (priority 1 overrides all)
- Developer's CLAUDE.md says "use semicolons" but Optima says "no semicolons" → Follow developer's CLAUDE.md (priority 1 > priority 2)
- Optima's CLAUDE.md says "use ESLint" but developer hasn't mentioned linting → Follow Optima (priority 2 applies)
- This file says "always call optima_get_context" but developer says "just fix this one line" → Call optima_get_context anyway (priority 3 instruction to call tools is not overridden by a task description — "just fix this" is a task, not an instruction to skip tools)

**The user is always in control.** If the developer explicitly says to stop using Optima, stop. But a casual "just fix it" is not an instruction to skip Optima — it's a task description.

## Optima Memory Rules

These rules ensure Optima learns from every session to improve future ones.

### After Fixing Errors

When you successfully resolve a compiler error, runtime exception, or failing test:
- You MUST call the `optima_memorize` tool before concluding your response.
- Include:
  - `type`: `"error_fix"`
  - `error`: The original error message (copy the key part, not the full stack trace)
  - `resolution`: A concise summary of what fixed it
  - `files`: Array of file paths that were modified
  - `directory`: The primary directory involved

### After Making Changes

After modifying code, always verify your work before reporting completion:
- Run the project's test command (if known from `optima_get_context`)
- Run the project's lint command (if known)
- If tests or lint fail, fix the issues before calling `optima_memorize`
- Only log the fix after verification passes

### After Architectural Decisions

When you make a significant design choice (new pattern, structural decision, technology choice):
- Call `optima_memorize` with:
  - `type`: `"architectural_rule"`
  - `rule`: The decision in one sentence
  - `rationale`: Why this approach was chosen
  - `directory`: The directory scope (or omit for project-wide)

### At Task Start

When beginning any task (bug fix, feature, refactor, test, review):
- Call `optima_get_context` with the relevant directory path
- Use the returned context (gotchas, rules, entities) to inform your approach
- Pay special attention to gotchas — they contain solutions to previously encountered errors

### After Establishing Patterns

When you establish a reusable code pattern or convention:
- Call `optima_memorize` with:
  - `type`: `"pattern"`
  - `pattern`: Description of the pattern
  - `example`: A brief code example
  - `directory`: Where this pattern applies

### After Completing a Task

When you finish a task (feature, bug fix, refactor, etc.) — regardless of whether it succeeded, partially succeeded, or was abandoned:
- Call `optima_memorize` with:
  - `type`: `"task_outcome"`
  - `task`: One-sentence description of what was attempted
  - `outcome`: `"success"`, `"partial"`, or `"abandoned"`
  - `learnings`: The key insight — what worked, what didn't, what was surprising. **This is the most valuable field.** Skip it only if the task was truly routine.
  - `duration_hint`: Rough time estimate (e.g., `"~45min"`, `"~2hr"`) — helps future estimation
  - `files`: Array of files that were modified
  - `directory`: The primary directory scope

**Why this matters:** Error fixes capture reactive knowledge ("this broke, here's the fix"). Task outcomes capture proactive knowledge ("this approach worked/didn't work for this kind of task"). Future sessions can use these insights to choose better approaches and estimate effort.

**When NOT to log:** Trivial tasks (rename a variable, fix a typo) don't need task outcomes. The threshold: would a future developer benefit from knowing how this task went?

## Iron Laws

These rules are non-negotiable. There are no exceptions. "Violating the letter of these rules is violating the spirit of these rules."

### Iron Law 1: Context Before Code

```
CALL optima_get_context BEFORE WRITING OR MODIFYING ANY CODE IN A DIRECTORY YOU HAVEN'T QUERIED THIS SESSION.
```

No exceptions. Not for "quick fixes." Not for "obvious changes." Not because "I already read CLAUDE.md." The tool gives you directory-specific context that CLAUDE.md cannot contain.

### Iron Law 2: Memorize Before Moving On

```
IF YOU FIXED AN ERROR, CALL optima_memorize WITH TYPE error_fix BEFORE MOVING TO THE NEXT TASK.
```

Not "after I finish everything." Not "I'll batch them." NOW. The moment the error is resolved and tests pass, store it. Future sessions depend on this.

### Iron Law 3: Evidence Before Claims

```
DO NOT CLAIM "CLAUDE.md UPDATED" OR "GOTCHA STORED" WITHOUT SHOWING THE TOOL'S RESPONSE.
```

If you called `optima_memorize`, show the `memory_id` from the response. If CLAUDE.md was regenerated, state whether the content hash changed or the write was skipped. No "should be updated" or "likely stored."

## Red Flags — STOP and Call Optima

If you catch yourself thinking any of these, STOP. You are about to skip Optima and you should not.

**Before writing code:**
- "I know what this error means" → STOP. Check gotchas first. A previous session may have spent 30 minutes debugging this exact error.
- "This directory is simple, no need for context" → STOP. Call `optima_get_context`. Simple directories still have architectural rules and patterns.
- "I already read CLAUDE.md, I have the context" → STOP. CLAUDE.md is a 35-instruction summary. The tool returns directory-specific entities, gotchas, and recent changes.
- "Let me just look at the code first" → STOP. Looking at code without Optima context means missing the gotchas that explain WHY the code looks that way.

**After fixing an error:**
- "I'll memorize this later" → STOP. Call `optima_memorize` NOW. You will forget. You will move on. The fix will be lost.
- "This fix is too simple to memorize" → STOP. Simple fixes are the ones developers repeat most often. Store it.
- "I'll batch all my memorizations at the end" → STOP. You'll forget half of them. Memorize each fix immediately.

**After memorizing:**
- "CLAUDE.md should be updated now" → STOP. Check the tool response. Did `claude_md_regenerated` return true? Did the content hash change? Evidence, not assumptions.

**General:**
- "This is different because..." → STOP. It is not different. Follow the Iron Laws.
- "I'm being pragmatic by skipping this" → STOP. Skipping Optima is not pragmatic. It's repeating work that previous sessions already did.
- "Just this once" → STOP. There is no "just this once." The rules apply every time.

## Rationalizations for Skipping Optima — All Invalid

Claude Code WILL try to skip calling Optima's tools. These are the excuses it will use and why they are all invalid:

| Claude Code's Excuse | Reality |
|---|---|
| "I already know this codebase" | You don't. Your context resets every session. Optima remembers across sessions. Call the tool. |
| "This is a quick fix, no need for context" | Quick fixes cause the most gotchas. Past developers stored fixes for exactly this scenario. Context takes <200ms. |
| "I'll memorize after I'm done" | You'll forget. You'll move to the next task and the fix will be lost. Memorize immediately when you resolve the error. |
| "The error is obvious, no need to check gotchas" | Obvious errors have non-obvious root causes. A previous session already debugged this for 30 minutes. Check before you repeat that work. |
| "optima_get_context is too slow" | It's <150ms when the index is fresh. You spend more time generating excuses than the tool takes to run. |
| "I'll just check CLAUDE.md instead of calling the tool" | CLAUDE.md is a project-wide summary with a 35-instruction cap. The tool returns directory-specific context, relevant gotchas, entity lists, and recent changes that CLAUDE.md cannot contain. |
| "I already called it for a parent directory" | Directory context is scoped. `src/auth/` has different gotchas than `src/`. Call it for the directory you're actually working in. |
| "This is a new file, there's no context to get" | The directory still has rules, patterns, and gotchas from adjacent files. The project still has architectural rules. Call the tool. |
| "I'm just reading code, not modifying it" | Reading without context means missing the gotchas that explain WHY the code looks that way. Call the tool. |
| "The developer didn't ask me to use Optima" | The developer installed Optima and configured it as an MCP server. That IS the instruction. You don't need per-task permission. |

## Tool Selection Decision Tree

When should you call which Optima tool?

```
Starting any task in a directory?
│
├── Have you called optima_get_context for this directory in this session?
│   ├── NO → Call optima_get_context first (Iron Law 1)
│   └── YES → Proceed with task
│
├── Just fixed an error?
│   └── YES → Call optima_memorize with type "error_fix" (Iron Law 2)
│
├── Discovered a codebase convention?
│   └── YES → Call optima_memorize with type "architectural_rule"
│
├── Noticed a recurring code pattern?
│   └── YES → Call optima_memorize with type "pattern"
│
├── Developer stated a preference?
│   └── YES → Call optima_memorize with type "preference"
│
├── Just finished a non-trivial task?
│   └── YES → Call optima_memorize with type "task_outcome"
│
├── Major refactor, dependency change, or directory restructure?
│   └── YES → Call optima_reindex
│
└── optima_get_context results seem stale (files exist that aren't in the index)?
    └── YES → Call optima_reindex
```

**When in doubt:** Call `optima_get_context`. It takes <200ms. The cost of calling unnecessarily is negligible. The cost of NOT calling is repeating work a previous session already did.
````

---

## Spirit vs. Letter Defense

**Source:** Improvement 10 (Superpowers anti-rationalization pattern)

The line "Violating the letter of these rules is violating the spirit of these rules" is placed as the **first thing Claude Code reads** in the feedback rules file. It preemptively shuts down an entire class of agent rationalizations where the agent claims to follow "the spirit" while skipping "the letter." Superpowers uses this in multiple skills and reports it is highly effective at preventing agent shortcuts.

This single line must remain at the top of the payload, before any other content.

---

## Instruction Priority

**Source:** Improvement 12

Defines the hierarchy when instructions conflict: developer explicit > Optima CLAUDE.md > feedback rules > Claude Code defaults. The critical nuance: a casual task description like "just fix it" is NOT an instruction to skip Optima — it's a task description. Only explicit opt-out ("don't use Optima for this") overrides the feedback rules.

This section is incorporated into the feedback rules payload so Claude Code knows the priority at tool-invocation time.

---

## Iron Laws

**Source:** Improvement 2 (Superpowers non-negotiable anchors pattern)

Three absolute rules that anchor agent behavior:
1. **Context Before Code** — Always call `optima_get_context` before modifying code in any directory not yet queried this session
2. **Memorize Before Moving On** — Call `optima_memorize` immediately after fixing an error, before starting the next task
3. **Evidence Before Claims** — Never claim "CLAUDE.md updated" or "gotcha stored" without showing the tool's `memory_id` or `claude_md_regenerated` response field

These rules are formatted as single-line imperatives in code blocks for maximum cognitive salience. They are incorporated into the feedback rules payload.

---

## Red Flags — STOP and Call Optima

**Source:** Improvement 3 (Superpowers self-monitoring pattern)

A checklist of internal thoughts that signal the agent is about to skip Optima. Each "red flag" thought is paired with a STOP instruction and the correct action. Organized by phase: before writing code, after fixing an error, after memorizing, and general patterns.

This section targets the moment of rationalization — catching the agent's internal reasoning before it becomes an action.

---

## Rationalizations for Skipping Optima — All Invalid

**Source:** Improvement 1 (Superpowers anti-rationalization engineering)

A table mapping every known rationalization Claude Code uses to skip Optima tools, paired with reality-check responses. This table was designed by anticipating how AI agents shortcut, then explicitly blocking those shortcuts.

Key rationalizations addressed:
- "I already know this codebase" (context resets every session)
- "This is a quick fix" (quick fixes cause the most gotchas)
- "I'll memorize later" (it won't — the fix will be lost)
- "The error is obvious" (obvious errors have non-obvious root causes)
- "CLAUDE.md has the context" (CLAUDE.md has a 35-instruction cap; the tool returns directory-specific data)

This table is incorporated into the feedback rules payload.

---

## Tool Selection Decision Tree

**Source:** Improvement 11

A text-based flowchart (compatible with `.claude/rules/` files which can't render graphviz) that maps situations to the correct Optima tool call. The tree covers:
- Starting any task → `optima_get_context`
- After fixing errors → `optima_memorize` (type: error_fix)
- After discovering conventions → `optima_memorize` (type: architectural_rule)
- After noticing patterns → `optima_memorize` (type: pattern)
- Developer preferences → `optima_memorize` (type: preference)
- Major restructures → `optima_reindex`
- Stale results → `optima_reindex`

Default guidance: "When in doubt, call `optima_get_context`. It takes <200ms."

This flowchart is incorporated into the feedback rules payload.

---

## File: `CLAUDE.md` (Section Markers)

Generated by `src/generator/claude-md.ts`. Uses section markers to avoid overwriting human-authored content. **Regeneration triggers:** (1) After every `optima_memorize` call (all types — error_fix, architectural_rule, pattern, preference, task_outcome). (2) After every `optima_reindex` call. (3) On cold start (first `optima_get_context`). Content hash check prevents unnecessary file writes — regeneration is cheap.

**Instruction budget constraint:** Optima sections should contribute no more than **30-40 instructions** total:
- `project_overview`: ~5 instructions
- `architecture_rules`: Cap at 10 rules
- `known_gotchas`: Cap at 10 gotchas
- `patterns`: Cap at 5 patterns
- `preferences`: Cap at 5 preferences (if any exist)
- `task_insights`: Cap at 5 task outcomes (only those with learnings)

**The necessity test (filtering rules for CLAUDE.md inclusion):**

1. **Gotchas:** Include only if `hit_count >= 2` (resolved Q4 — a gotcha retrieved twice has proven value). This is the single authoritative threshold. All other gotcha filters are subsumed by this rule:

   | `hit_count` | Age | CLAUDE.md? | Reason |
   |---|---|---|---|
   | 0 | Any | ❌ Exclude | Never retrieved — unproven |
   | 1 | Any | ❌ Exclude | Retrieved once — not yet proven |
   | 2+ | Any | ✅ Include | Retrieved 2+ times — proven value |

   **Retention:** All gotchas remain in the database regardless of hit_count (for future matching). The filter only affects CLAUDE.md generation output.

2. **Rules:** Exclude rules that duplicate linter config (if `linterDetected` is non-null, skip style/formatting rules). Exclude obvious conventions the model knows from general training.
3. **Patterns:** Exclude patterns with only 1 recorded occurrence (not yet established).
4. **Preferences:** Include all (user explicitly stored these; they are always intentional).
5. **Task outcomes:** Include only outcomes with non-null `learnings`. Sort by `created_at` DESC (most recent first). Cap at 5.

**Instruction budget enforcement algorithm:**
1. Query all gotchas/rules/patterns/preferences from database.
2. Apply necessity test filters above.
3. Sort gotchas by `hit_count` DESC, then `updated_at` DESC.
4. Sort rules by `created_at` DESC (most recent first).
5. Sort patterns by occurrence count DESC (if tracked), else `created_at` DESC.
6. Truncate each category to its cap: 10 rules, 10 gotchas, 5 patterns, 5 preferences, 5 task insights.
7. If total instructions > 40: reduce lowest-priority items from largest section first.
8. Sections with zero entries after filtering: omit entirely (no empty headers).

**Domain-aware framing:** Extract project purpose from package.json → pyproject.toml → README.md first paragraph.

**Marker format:**

```markdown
<!-- OPTIMA:START section_id -->
(generated content here)
<!-- OPTIMA:END section_id -->
```

**Sections (in order):**

### Section: `project_overview`

```markdown
<!-- OPTIMA:START project_overview -->
## Project Overview (Optima-managed)

- **Name**: {{project_name}}
{{#if project_purpose}}
- **Purpose**: {{project_purpose}}
{{/if}}
- **Tech Stack**: {{tech_stack_comma_separated}}
- **Build**: `{{build_command}}`
- **Test**: `{{test_command}}`
- **Lint**: `{{lint_command}}`
<!-- OPTIMA:END project_overview -->
```

### Section: `architecture_rules`

```markdown
<!-- OPTIMA:START architecture_rules -->
## Architecture Rules (Optima-managed)

{{#each rules where type = "architectural_rule"}}
- **{{rule.content}}** {{#if rule.rationale}}— {{rule.rationale}}{{/if}} {{#if rule.directory}}(scope: `{{rule.directory}}`){{/if}}
{{/each}}
<!-- OPTIMA:END architecture_rules -->
```

### Section: `known_gotchas`

```markdown
<!-- OPTIMA:START known_gotchas -->
## Known Gotchas (Optima-managed)

These are errors previously encountered and resolved in this project.

{{#each top_gotchas sorted by hit_count desc, limit 10}}
- **`{{gotcha.error_text}}`** → {{gotcha.resolution}} {{#if gotcha.root_cause}}(Root cause: {{gotcha.root_cause}}){{/if}} {{#if gotcha.possibly_outdated}}**(recorded against {{gotcha.dependency_version}} — current version differs, may be outdated)**{{/if}} ({{gotcha.files}})
{{/each}}
<!-- OPTIMA:END known_gotchas -->
```

### Section: `patterns`

```markdown
<!-- OPTIMA:START patterns -->
## Code Patterns (Optima-managed)

{{#each rules where type = "pattern"}}
- **{{rule.content}}** {{#if rule.example}}
\`\`\`
{{rule.example}}
\`\`\`
{{/if}}
{{/each}}
<!-- OPTIMA:END patterns -->
```

### Section: `preferences`

```markdown
<!-- OPTIMA:START preferences -->
## Developer Preferences (Optima-managed)

{{#each rules where type = "preference"}}
- {{rule.content}} {{#if rule.rationale}}— {{rule.rationale}}{{/if}}
{{/each}}
<!-- OPTIMA:END preferences -->
```

**Note:** If no preferences exist, this section is omitted entirely.

### Section: `task_insights`

```markdown
<!-- OPTIMA:START task_insights -->
## Past Task Insights (Optima-managed)

These are outcomes from previous tasks in this project — what worked, what didn't, and how long things took.

{{#each recent_task_outcomes sorted by created_at desc, limit 5}}
- **{{task.task}}** — {{task.outcome}} {{#if task.duration_hint}}({{task.duration_hint}}){{/if}} {{#if task.learnings}}
  *Insight:* {{task.learnings}}{{/if}} {{#if task.directory}}(scope: `{{task.directory}}`){{/if}}
{{/each}}
<!-- OPTIMA:END task_insights -->
```

**Note:** If no task outcomes exist, this section is omitted entirely. Cap at 5 entries (most recent first). Only include outcomes with non-null `learnings` — outcomes without learnings don't add value to CLAUDE.md.

---

## CLAUDE.md Merge — Edge Case Scenarios

### Scenario 1: First Run (No CLAUDE.md exists)
- Create new CLAUDE.md with all Optima sections and markers
- No merge needed — write the full file

### Scenario 2: CLAUDE.md Exists with No Optima Markers
- Developer has a hand-written CLAUDE.md
- Append all Optima sections at the bottom of the file with markers
- Do NOT touch any existing content above the first Optima marker
- Log: "Existing CLAUDE.md found without Optima markers. Optima sections appended at bottom."

### Scenario 3: Developer Deletes All Optima Markers
- CLAUDE.md exists but no `<!-- OPTIMA:START -->` or `<!-- OPTIMA:END -->` markers found
- Treat entire file as human-owned
- Append new Optima sections at bottom with fresh markers
- Log warning: "Optima markers were removed from CLAUDE.md. New Optima content appended at bottom."

### Scenario 4: Developer Edits Content Inside Optima Markers

**Trigger:** Content hash of a section's inner content differs from the hash stored in `generation_log` for that section.

**Behavior:**
1. Detect the mismatch: compute SHA-256 of the text between `<!-- OPTIMA:START section_id -->` and `<!-- OPTIMA:END section_id -->`. Compare against `generation_log.content_hash` for that `section_id`.
2. If hashes differ, the developer has edited Optima-managed content.
3. **DO NOT overwrite.** Keep the developer's edited content.
4. Append the new generated content below the developer's version, separated by a merge comment:
   ```markdown
   <!-- OPTIMA:MERGE_CONFLICT section_id timestamp -->
   ```
5. The developer reconciles manually. On next regeneration, if the merge comment still exists, Optima skips that section entirely (the developer hasn't resolved it yet).
6. Log: `"Merge conflict detected in section '${section_id}'. Developer edits preserved. New content appended below merge marker."`

**Zero data loss guarantee:** Both the developer's edits and Optima's new content are visible. Neither is silently dropped.

### Scenario 5: Developer Moves Optima Sections to Different Positions

**Trigger:** Optima sections exist in CLAUDE.md but in a different order than the canonical order (project_overview → architecture_rules → known_gotchas → patterns → preferences).

**Behavior:**
1. Sections are found by their `section_id` in the marker comment, NOT by position in the file.
2. If the developer reorders sections (e.g., puts `known_gotchas` before `architecture_rules`), Optima respects the developer's order.
3. Optima updates each section in-place at its current position. It does NOT re-sort sections to canonical order.
4. New sections (e.g., `preferences` section appearing for the first time) are appended after the last existing Optima section, wherever that may be in the file.

**Rationale:** The developer's file organization preferences take priority. Optima owns the content inside markers, not the ordering of markers within the file.

### Scenario 6: Concurrent Write Detection

**Trigger:** Another process (developer's editor, git operation, another Claude Code session) modifies CLAUDE.md between Optima's read and write.

**Behavior:**
1. Before writing, check the file's mtime against the mtime captured at read time.
2. If mtime has changed (file was modified by another process between read and write):
   a. Abort the write.
   b. Re-read the file.
   c. Re-merge (re-run the full section parsing and content generation against the new file state).
   d. Retry the write.
3. Retry up to 3 times with a 100ms delay between retries.
4. If still failing after 3 retries:
   a. Log warning: `"CLAUDE.md concurrent write detected. Skipping regeneration after 3 retries. Will retry on next tool call."`
   b. Skip this regeneration cycle. The next `optima_memorize` or `optima_reindex` call will trigger another attempt.

**Implementation:**
```typescript
async function safeWriteClaudeMd(path: string, content: string, maxRetries = 3): Promise<boolean> {
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    const readMtime = (await fs.stat(path)).mtimeMs;
    const merged = mergeClaudeMd(await fs.readFile(path, 'utf-8'), content);
    
    // Check if file was modified during merge
    const currentMtime = (await fs.stat(path)).mtimeMs;
    if (currentMtime !== readMtime) {
      // File changed during merge — retry
      await new Promise(r => setTimeout(r, 100));
      continue;
    }
    
    await fs.writeFile(path, merged, 'utf-8');
    return true;
  }
  
  // All retries exhausted
  return false;
}
```

---

## Regeneration Logic

1. Read existing `CLAUDE.md` content.
2. For each section ID, find and replace content between markers.
3. If markers don't exist, append section at end.
4. Content outside markers is never modified.
5. Compute SHA-256 hash and skip write if unchanged.

**Malformed marker handling:**
- **Orphaned START without END:** Replace from START to next marker/EOF with full section.
- **Orphaned END without START:** Delete orphaned END, append full section with both markers.
- **Duplicate markers:** Update only the FIRST pair, delete duplicates.
- **Whitespace variations:** Match with `<!--\s*OPTIMA:START\s+(\w+)\s*-->`.

**Empty sections:** Omit entire section if zero entries.

---

## Project Purpose Extraction Priority

Used by `src/indexer/project-analyzer.ts` to populate `projectMeta.projectPurpose`. Copy this logic verbatim.

```typescript
import { readFile } from "node:fs/promises";
import { join } from "node:path";

export async function extractProjectPurpose(projectRoot: string): Promise<string | null> {
  // Priority 1: package.json description
  try {
    const pkg = JSON.parse(await readFile(join(projectRoot, "package.json"), "utf-8"));
    if (typeof pkg.description === "string" && pkg.description.trim().length > 0) {
      return pkg.description.trim();
    }
  } catch { /* no package.json or invalid JSON */ }

  // Priority 2: pyproject.toml [project].description
  try {
    const toml = await readFile(join(projectRoot, "pyproject.toml"), "utf-8");
    const match = toml.match(/\[project\][^[]*?description\s*=\s*"([^"]+)"/s);
    if (match?.[1]) {
      return match[1].trim();
    }
  } catch { /* no pyproject.toml */ }

  // Priority 3: README.md first non-heading paragraph
  try {
    const readme = await readFile(join(projectRoot, "README.md"), "utf-8");
    const lines = readme.split("\n");
    for (const line of lines) {
      const trimmed = line.trim();
      // Skip empty lines, headings, badges, HTML tags
      if (!trimmed) continue;
      if (trimmed.startsWith("#")) continue;
      if (trimmed.startsWith("![")) continue;
      if (trimmed.startsWith("<")) continue;
      if (trimmed.startsWith("---")) continue;
      // Found first content paragraph
      return trimmed.length > 200 ? trimmed.slice(0, 200) + "..." : trimmed;
    }
  } catch { /* no README.md */ }

  return null;
}
```

If none found, `projectPurpose` is `null` in the database and `purpose` is `null` in tool output.

---

## File: `.gitignore` Addition

Append (if not present):

```
# Optima local database
.optima/
```

---

## File: `.optima/.gitignore`

Create with:

```
*
```

---

## Recommended Tool Output Presentation

When Claude Code receives output from Optima's tools, the `.claude/rules/optima-feedback.md` file instructs it how to use the data. However, the presentation of that data to the developer is Claude Code's responsibility. These recommendations help Claude Code present Optima's context effectively.

### Presenting optima_get_context Output

**Gotchas:** When gotchas are present in the context, Claude Code should check them BEFORE attempting to fix a similar error. Present relevant gotchas inline:

```
Optima found a previously resolved issue similar to this:
   Error: `Cannot read property 'id' of undefined`
   Resolution: Add null-check after fetchUser() — session cookies can expire
   Files: src/auth/login.ts
```

**Rules:** Architecture rules should be treated as constraints, not suggestions. Claude Code should follow them without asking for confirmation.

**Entities:** When the entity list is large, Claude Code should filter to entities relevant to the current task rather than dumping the full list.

**Recent Changes:** If recent_changes is non-empty, Claude Code should mention which files were just re-indexed, as this gives the developer visibility into what Optima detected.

### Presenting optima_memorize Output

After storing knowledge, Claude Code should confirm briefly:

```
Stored error fix for future reference (gotcha ID: abc-123)
```

Do NOT show the full stored object. The developer doesn't need to see the normalized hash or internal IDs.

### Presenting optima_reindex Output

```
Project re-indexed: 142 files scanned, 87 entities found (2.3s)
```

---

## Directory: `.optima/`

Contains:
- `optima.db` — SQLite database
- `.gitignore` — Ignore all contents
